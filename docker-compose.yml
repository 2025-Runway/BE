version: "3"

services:
  backend:
    image: runrunway/backend
    container_name: backend
    hostname: backend
    env_file:
      - .env
    expose:
      - "8080"

  nginx:
    image: runrunway/nginx
    depends_on:
      - backend
    restart: always
    ports:
      - "80:80"
